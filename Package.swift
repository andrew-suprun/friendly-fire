// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "friendly-fire",
    dependencies: [
    ],
    targets: [
        .target(
            name: "friendly-fire",
            dependencies: []),
        .target(
          name: "cli",
          dependencies: [
          ]),
    ]
)
